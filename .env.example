# ─────────────────────────────────────────────────────────────────────────────
# Waves environment. Copy to `.env` and fill in. NEVER commit a real .env.
# ADR-013 / TDR §11: service and LLM keys live ONLY in edge-function env,
# never in the mobile bundle. Anything the app may read must be EXPO_PUBLIC_*.
# ─────────────────────────────────────────────────────────────────────────────

# Prisma (TDR §2.0) — pooled for runtime queries, direct for migrations.
# Local dev via `pnpm db:pg:up` (both point at the same throwaway container):
DATABASE_URL="postgresql://postgres:postgres@localhost:54330/waves?schema=public"
DIRECT_URL="postgresql://postgres:postgres@localhost:54330/waves?schema=public"
# Supabase production shape:
# DATABASE_URL="postgresql://postgres.<ref>:<pw>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.<ref>:<pw>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# Mobile app (safe to ship — anon key is RLS-scoped by design)
EXPO_PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
EXPO_PUBLIC_SUPABASE_ANON_KEY=""

# Personal-cloud receipt backup (A34). PUBLIC installed-app OAuth client ids —
# PKCE flow, so no client secret ships in the app; blank disables that provider
# (the Backup settings screen shows "not set up"). Register one app per provider
# on its console with redirect URI `waves://oauthredirect`:
#   - Google Cloud console → OAuth client (Android/iOS), Drive API enabled
#   - Dropbox App Console → scoped app, files.content.write
#   - Microsoft Entra (Azure) → app registration, Files.ReadWrite.AppFolder
EXPO_PUBLIC_CLOUD_GDRIVE_CLIENT_ID=""
EXPO_PUBLIC_CLOUD_DROPBOX_CLIENT_ID=""
EXPO_PUBLIC_CLOUD_ONEDRIVE_CLIENT_ID=""

# Edge functions only — never referenced from apps/*
SUPABASE_SERVICE_ROLE_KEY=""
ANTHROPIC_API_KEY=""
RESEND_API_KEY=""
RESEND_WEBHOOK_SECRET=""

# Developer API (apps/api) -- the public API third-party software calls. Its own
# apps/api/.env.example is the full list with the reasoning; these are the two
# secrets, repeated here because this file is where the repo keeps that
# inventory. Neither is the service-role key: the API server holds none.
#
# The project's JWT secret. Signs a ~60-second `authenticated` JWT for the person
# an API token names, so every request runs under their own RLS.
WAVES_API_JWT_SECRET=""
# Signs the API's own credentials (tokens, OAuth codes). Rotating it invalidates
# every issued token at once, which is what you want in an incident.
WAVES_API_TOKEN_SECRET=""
