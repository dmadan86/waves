node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# expo
.expo/
.expo-shared/
web-build/
apps/mobile/ios/
apps/mobile/android/

# env & secrets (ADR-013: keys only in edge-function env)
.env
.env.*
!.env.example

# A production connection string, held only long enough to run
# `prisma migrate deploy` against it. Never committed, never printed.
.prod-url

# prisma
packages/db/generated/

# supabase local stack
supabase/.branches/
supabase/.temp/

# os / editor
.DS_Store
Thumbs.db
*.log
coverage/

# local web export used for visual checks
.tmp-web-export/

# Play Store service-account key, written from a CI secret at release time
google-service-account.json
**/google-service-account.json

# generated: core bundled for the Deno edge runtime
supabase/functions/_shared/core.js
packages/core/dist/

# Shell artefacts: an unquoted JS arrow in a terminal command (`=> row.id`)
# is parsed as a redirect and silently creates an empty file. These are always
# accidents, never source.
#
# They are deliberately NOT ignored. The patterns that used to live here (`*)`,
# `{`, `({`) matched directories as well as files: `*)` swallowed the whole
# `app/(tabs)/` directory, so `people.tsx` was created, ran locally forever, and
# was never committed — the tab bar shipped pointing at a route that did not
# exist in the repo. A stray empty file showing up in `git status` is noise you
# delete in one command; a source directory silently missing from the repo is a
# feature that disappears on somebody else's clone.

# Next.js
.next/
out/
next-env.d.ts

# Vercel CLI link (project ids, not secrets — still not ours to commit)
.vercel

# Playwright (web e2e)
apps/web/playwright-report/
apps/web/test-results/
apps/web/blob-report/
apps/web/playwright/.cache/

.vercel
# A second, broader .env rule than the one at the top of this file, added with
# the Vercel apps. It swallowed `!.env.example` along with everything else, so
# the exception has to be restated after it or a new app's example file is
# silently untracked (apps/api's was).
.env*
!.env.example
